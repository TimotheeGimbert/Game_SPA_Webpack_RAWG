export { pageDetailLoading };

const pageDetailLoading = () => {
  return `
    <main class="page-detail">
      <section class="hero">
        <button>Check Website |></button> 
      </section>
    </main>
  `;
};